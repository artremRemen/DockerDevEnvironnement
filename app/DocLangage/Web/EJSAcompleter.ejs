npm install ejs                 // sert a installer la librairie ejs


sert a coder une page en HTML classic en prennent a compte le langage EJS en plus doit etre dans le dossier view("racine")      qui doit etre dans le dossier racine du site

<%=  attribut %>         //sert a afficher une variable dans la page EJS    

<% code %>        //inclure un fichier dans un autre pour ne pas devoir repeter le code pour rien

locals.var                      //sert a appeller une variable locale (utilisée dans un autre page par exp )

<%- include('chemin') %>        //inclure un ficher dans un autre par soin chemin d'access